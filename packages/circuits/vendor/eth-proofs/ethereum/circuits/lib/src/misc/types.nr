pub global ADDRESS_LENGTH: u32 = 20;
pub type Address = [u8; ADDRESS_LENGTH];

pub global BYTES32_LENGTH: u32 = 32;
pub type Bytes32 = [u8; BYTES32_LENGTH];

pub global HASH_LEN: u32 = 32;
pub type Hash = Bytes32;

pub global U128_LENGTH: u32 = 16;
